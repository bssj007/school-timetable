name = "school-timetable"
compatibility_date = "2024-12-20"
pages_build_output_dir = "dist/public"

# D1 데이터베이스 바인딩
[[d1_databases]]
binding = "DB"
database_name = "school-timetable-db"
database_id = "7367c8d6-a8a7-41fc-8b97-742890a5bdce"

# NOTE: Cloudflare Pages does not support cron triggers in wrangler.toml
# Cron must be configured in Cloudflare Dashboard:
# Workers & Pages > school-timetable > Settings > Functions > Cron Triggers
